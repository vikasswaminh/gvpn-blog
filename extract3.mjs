import fs from 'fs'; for(let i=1;i<=5;i++){try{const r=JSON.parse(fs.readFileSync('./lh-prod-final-'+i+'.json'));const items=r.audits['lcp-breakdown-insight'].details.items;const table=items[0].items;let ttfb=0,ld=0,ldur=0,rd=0;table.forEach(t=>{if(t.subpart==='timeToFirstByte')ttfb=t.duration;if(t.subpart==='resourceLoadDelay')ld=t.duration;if(t.subpart==='resourceLoadDuration')ldur=t.duration;if(t.subpart==='elementRenderDelay')rd=t.duration;});console.log('Run '+i+': TTFB(ms):'+ttfb.toFixed(1)+' LoadDelay(ms):'+ld.toFixed(1)+' LoadDuration(ms):'+ldur.toFixed(1)+' RenderDelay(ms):'+rd.toFixed(1));}catch(e){console.log('Error run',i,e.message);}}
