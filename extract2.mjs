import fs from 'fs'; for(let i=1;i<=5;i++){try{const r=JSON.parse(fs.readFileSync('./lh-prod-final-'+i+'.json'));const a=r.audits;const lcp=a['largest-contentful-paint-element'].details.items[0];const lcpBreakdown = r.audits['lcp-breakdown'] ? r.audits['lcp-breakdown'].details.items : 'not found';console.log('Run '+i+' LCP Element:',lcp?lcp.node.snippet:'None');if(lcpBreakdown !== 'not found' && lcpBreakdown.length > 0){console.log('  TTFB:',lcpBreakdown[0].ttfb,'Load Delay:',lcpBreakdown[0].loadDelay,'Load Duration:',lcpBreakdown[0].loadDuration,'Render Delay:',lcpBreakdown[0].renderDelay);}}catch(e){console.log('Error run',i,e.message);}}
