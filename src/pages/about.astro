---
import BaseLayout from '../layouts/BaseLayout.astro';
import { SITE } from '../config';
          Looking for the product itself? Head to
          <a href={SITE.marketingUrl}>{SITE.marketingLabel}</a>.
        </p>
      </div>
    </article>
  </div>
</BaseLayout>

<style>
  .about-page {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 60vh;
    text-align: center;
  }
  .about-page .prose {
    margin: 0 auto;
  }
</style>
